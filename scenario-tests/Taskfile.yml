version: "3"

dotenv:
  - 'profiles/{{.PROFILE}}.env'
  - '.env'

includes:
  common:
    taskfile: Taskfile.common.yml
    flatten: true
  local:
    taskfile: Taskfile.local.yml
    flatten: true
    optional: true

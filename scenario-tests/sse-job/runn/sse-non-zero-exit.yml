desc: SSE job non-zero exit failed
vars:
steps:
  test-sse:
    include:
      path: sse-included.yml
      vars:
        device_id: ${DEVICE_ID}
        job_name: sse-non-zero-exit-failed
        input_payload_path: payloads/sse-non-zero-exit.zip
        expected_status: "failed"
        expected_transpiler_lib: qiskit
        expected_transpiler_options:
          optimization_level: 1
        pass_transpiler_result_check: true
        expected_execution_time_filled: true
        expected_result_present: false
        expected_sse_log_present: false

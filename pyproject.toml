[project]
name = "test-suite-docs"
version = "0.1.0"
description = "Documentation for test-suite"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
    "mkdocs-include-markdown-plugin>=6.0.0",
]

# Benchmark test environment variables (template)
# Copy to .env and fill in real values. Do NOT commit the filled-in .env.

# User-API base URL, e.g. https://api.example.com
USER_API_ENDPOINT="https://YOUR_END_POINT"
# API token sent as the q-api-token header
Q_API_TOKEN="YOUR_API_TOKEN"
# Target device id
DEVICE_ID="qulacs"

# Note: benchmark thresholds are NOT set here. They live in `thresholds.toml`.
